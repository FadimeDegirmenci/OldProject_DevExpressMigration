<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="SM.MAUI.Views.ProductListPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:SM.MAUI.Converters"
             Title="Urun Listesi">

    <ContentPage.Resources>
        <converters:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        <converters:StringToBoolConverter x:Key="StringToBoolConverter" />
    </ContentPage.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Grid Grid.Row="0" Padding="15" BackgroundColor="#2E86AB">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" 
                   Text="Ürün Listesi" 
                   FontSize="20" 
                   FontAttributes="Bold" 
                   TextColor="White"
                   VerticalOptions="Center" />

            <Button Grid.Column="1"
                    Text="🔄"
                    BackgroundColor="#28A745"
                    TextColor="White"
                    CornerRadius="8"
                    Margin="5,0"
                    WidthRequest="40"
                    HeightRequest="40"
                    FontSize="16"
                    Command="{Binding RefreshCommand}" />

            <Button Grid.Column="2"
                    Text="➕"
                    BackgroundColor="#17A2B8"
                    TextColor="White"
                    CornerRadius="8"
                    Margin="5,0"
                    WidthRequest="40"
                    HeightRequest="40"
                    FontSize="16"
                    Command="{Binding AddProductCommand}" />

            <Button Grid.Column="3"
                    Text="🏠"
                    BackgroundColor="#FFC107"
                    TextColor="#495057"
                    CornerRadius="8"
                    WidthRequest="40"
                    HeightRequest="40"
                    FontSize="16"
                    Clicked="OnBackToMainClicked" />
        </Grid>

        <!-- Loading Indicator -->
        <ActivityIndicator Grid.Row="1"
                          IsVisible="{Binding IsBusy}"
                          IsRunning="{Binding IsBusy}"
                          Color="#2E86AB"
                          VerticalOptions="Center"
                          HorizontalOptions="Center"
                          HeightRequest="60"
                          WidthRequest="60" />

        <!-- Error Message -->
        <Frame Grid.Row="1"
               BackgroundColor="#F8D7DA"
               BorderColor="#DC3545"
               CornerRadius="10"
               Padding="20"
               Margin="20"
               IsVisible="{Binding HasError}"
               VerticalOptions="Center"
               HasShadow="False">
            <StackLayout>
                <Grid ColumnDefinitions="Auto,*">
                    <Label Grid.Column="0" Text="❌" FontSize="24" VerticalOptions="Center" />
                    <Label Grid.Column="1" Text="{Binding ErrorMessage}"
                           TextColor="#721C24"
                           FontSize="16"
                           VerticalOptions="Center"
                           Margin="10,0,0,0"
                           LineBreakMode="WordWrap" />
                </Grid>
                <Button Text="🔄 Tekrar Dene" 
                        BackgroundColor="#DC3545"
                        TextColor="White"
                        CornerRadius="8"
                        Margin="0,15,0,0"
                        Command="{Binding RefreshCommand}" />
            </StackLayout>
        </Frame>

        <!-- Products List -->
        <CollectionView Grid.Row="1" 
                       ItemsSource="{Binding Products}"
                       IsVisible="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"
                       BackgroundColor="#F8F9FA"
                       Margin="10,5">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="5">
                        <Frame BackgroundColor="White" 
                               CornerRadius="12" 
                               HasShadow="True"
                               Padding="15"
                               Margin="0,5">

                            <!-- Gesture Recognizers -->
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ProductTappedCommand}"
                                                    CommandParameter="{Binding .}" />
                            </Frame.GestureRecognizers>

                            <!-- Context Menu -->
                            <FlyoutBase.ContextFlyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="👁️ Detayları Gör" 
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ProductTappedCommand}"
                                                    CommandParameter="{Binding .}" />
                                    <MenuFlyoutSeparator />
                                    <MenuFlyoutItem Text="🗑️ Sil" 
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteProductCommand}"
                                                    CommandParameter="{Binding .}" />
                                </MenuFlyout>
                            </FlyoutBase.ContextFlyout>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Urun Adi -->
                                <Label Grid.Row="0" Grid.Column="0"
                                       Text="{Binding Name}"
                                       FontSize="18"
                                       FontAttributes="Bold"
                                       TextColor="#212529"
                                       LineBreakMode="TailTruncation" />

                                <!-- Fiyat -->
                                <Frame Grid.Row="0" Grid.Column="1"
                                       BackgroundColor="#E8F5E8"
                                       CornerRadius="8"
                                       Padding="8,4"
                                       HasShadow="False"
                                       VerticalOptions="Center">
                                    <Label Text="{Binding Price, StringFormat='{0:F2} TL'}"
                                           FontSize="14"
                                           FontAttributes="Bold"
                                           TextColor="#28A745" />
                                </Frame>

                                <!-- SKU -->
                                <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="0,8,0,0" ColumnDefinitions="Auto,*">
                                    <Label Grid.Column="0" Text="🔖" FontSize="14" VerticalOptions="Center" />
                                    <Label Grid.Column="1" Text="{Binding SKU}"
                                           FontSize="14"
                                           FontAttributes="Bold"
                                           TextColor="#007ACC"
                                           VerticalOptions="Center"
                                           Margin="5,0,0,0" />
                                </Grid>

                                <!-- Aciklama -->
                                <Label Grid.Row="2" Grid.ColumnSpan="2"
                                       Text="{Binding Description}"
                                       FontSize="12"
                                       TextColor="#6C757D"
                                       LineBreakMode="TailTruncation"
                                       MaxLines="2"
                                       Margin="0,5,0,0" />

                                <!-- Alt Bilgi -->
                                <Grid Grid.Row="3" Grid.ColumnSpan="2" Margin="0,8,0,0" ColumnDefinitions="Auto,*,Auto">
                                    <Label Grid.Column="0" Text="📅" FontSize="12" VerticalOptions="Center" />
                                    <Label Grid.Column="1" Text="{Binding CreatedDate, StringFormat='{0:dd.MM.yyyy}'}"
                                           FontSize="11"
                                           TextColor="#6C757D"
                                           VerticalOptions="Center"
                                           Margin="5,0,0,0" />
                                    <Label Grid.Column="2" Text="👆 Uzun basın"
                                           FontSize="10"
                                           TextColor="#6C757D"
                                           FontAttributes="Italic"
                                           VerticalOptions="Center" />
                                </Grid>
                            </Grid>
                        </Frame>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>

            <CollectionView.EmptyView>
                <Frame BackgroundColor="White"
                       CornerRadius="15"
                       Padding="30"
                       Margin="20"
                       HasShadow="True"
                       VerticalOptions="Center">
                    <StackLayout>
                        <Label Text="📦" FontSize="48" HorizontalOptions="Center" />
                        <Label Text="Henüz ürün yok" 
                               FontSize="18" 
                               FontAttributes="Bold"
                               TextColor="#6C757D"
                               HorizontalOptions="Center"
                               Margin="0,10,0,0" />
                        <Label Text="API'den ürün listesi alınamadı veya henüz ürün eklenmedi." 
                               FontSize="14"
                               TextColor="#6C757D"
                               HorizontalOptions="Center"
                               HorizontalTextAlignment="Center"
                               Margin="0,10,0,20" />
                        <Button Text="🔄 Yeniden Dene" 
                                BackgroundColor="#28A745"
                                TextColor="White"
                                CornerRadius="20"
                                HeightRequest="45"
                                Command="{Binding RefreshCommand}" />
                    </StackLayout>
                </Frame>
            </CollectionView.EmptyView>
        </CollectionView>
    </Grid>

</ContentPage>